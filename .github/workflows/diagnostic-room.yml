name: Temporary room diagnostic

on:
  push:
    branches:
      - mmm-mvp-0.1-core
    paths:
      - .github/workflows/diagnostic-room.yml
  workflow_dispatch:

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - name: Inspect room 123123
        shell: bash
        run: |
          set -euo pipefail
          echo "STATUS:"
          curl -fsS -i https://mememe-online.lengochung28191.workers.dev/api/rooms/123123/status || true
