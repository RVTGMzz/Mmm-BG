name: Publish compiled GitHub playtest manual fallback

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic playtest publishing now runs as the final step of MMM MVP CI."
