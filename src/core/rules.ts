// PoC-only defaults for MVP 0.1.5.
// These are intentionally isolated so they can be changed without touching card/runtime code.
// They are NOT locked final MeMeMe rules.
export const MVP_CARD_HAND_LIMIT = 3;
export const MVP_MAX_CARD_PLAYS_PER_TURN = 1;
