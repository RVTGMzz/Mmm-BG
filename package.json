{
  "name": "mememe-boardgame",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:playtest": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test:replay": "tsx tests/replay-determinism.ts",
    "test:lockstep": "tsx tests/lockstep-peer.ts",
    "test:host-client": "tsx tests/host-client-queue.ts",
    "test:authority": "tsx tests/authority-protocol.ts",
    "test:two-tab": "tsx tests/two-tab-session.ts",
    "test:demo-shell": "tsx tests/demo-match-shell.ts",
    "test:package": "node scripts/verify-playtest-package.mjs",
    "preview": "vite preview",
    "preview:playtest": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vite": "^7.1.5"
  }
}
